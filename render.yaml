services:
  - type: web
    name: aibtc-cyber-comet
    runtime: node
    buildCommand: npm install
    startCommand: npm run start:web
    envVars:
      - key: CLIENT_MNEMONIC
        sync: false
      - key: NODE_ENV
        value: production
